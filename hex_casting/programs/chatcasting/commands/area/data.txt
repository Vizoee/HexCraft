list:
    [0]config:
        [0]active_area_name: string
        [1]dynamic_size: boolean
        [2]last_look_pos: vec
    [1]areas: dict - name as key
        area:
            [0]settings:
                is_visible: boolean
                color: pigment
                binded_area: area_name
            [1]corners: list


commands:
area

new <name>
    creates new area and sets it to active
    informs if area exists

remove <name>
    if isVisible:
        removes mesh

active <name>
    sets 

color <name>
    sets active area color

set_pos
    sets position of block you are looking at

shift
    adds 1 block to axis you are looking at
    when shifted removes 1 block to axis you are looking at
    on cad

